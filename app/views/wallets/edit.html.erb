<div class="container">
  <%= simple_form_for @wallet do |f| %>
    <%= render :partial=> 'form', :locals=> {:f=> f}  %>
    <%= f.button :submit, 'save wallet' %> or <%= link_to 'cancel', wallets_path %>
  <% end %>
</div>