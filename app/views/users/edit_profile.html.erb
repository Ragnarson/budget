<%= simple_form_for @user, html: {class: "form-horizontal"} do |f| %>

  <div class="form-inputs">
    <%= f.input :locale, collection: [['Polski','pl'], ['English','en']], include_blank: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
